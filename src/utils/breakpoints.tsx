const breakpoints = {
    tablet: "767px",
    desktop: "1024px"
}

const screens = {
    md: `(min-width: ${breakpoints.tablet})`,
    lg: `(min-width: ${breakpoints.desktop})`,
}

export default screens;